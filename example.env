# Required for Google Cloud Translation
GCP_PROJECT_ID=your-google-cloud-project-id
GCP_LOCATION=global
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# Required for OpenAI (if using openai provider)
OPENAI_API_KEY=your-openai-api-key
OPENAI_MODEL=gpt-3.5-turbo # Optional, defaults to gpt-3.5-turbo

# Choose which translation provider to use (e.g., "google", "openai", "dummy")
# Use "dummy" for testing without actual API calls.
TRANSLATION_PROVIDER=dummy